# When true (default), the app reads from src/mock/mockData.ts.
# When false, it calls VITE_API_URL/api/snapshot for real data.
VITE_USE_MOCK_DATA=true

# URL of the Go backend. Only used when VITE_USE_MOCK_DATA=false.
VITE_API_URL=http://localhost:8080
